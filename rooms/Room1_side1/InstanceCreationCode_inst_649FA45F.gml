container = [convertTo("name", vfx_bubble), convertTo("name", vfx_bubble), convertTo("name", vfx_blood), convertTo("name", obj_ice_fish), convertTo("name", obj_Depthometer)];
//chest duplicating catachall
if (place_meeting(x,y, obj_chest)) {
	instance_destroy();
}
