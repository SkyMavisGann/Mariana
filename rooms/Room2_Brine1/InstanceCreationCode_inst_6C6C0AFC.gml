container = [convertTo("name", obj_Wet_Burrito), convertTo("name", obj_Mauler_Fin), convertTo("name", obj_Mauler_Fin)];
//chest duplicating catachall
if (place_meeting(x,y, obj_chest)) {
	instance_destroy();
}
